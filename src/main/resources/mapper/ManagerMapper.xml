<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.powernode.springboot.mapper.database.ManagerMapper">
    <insert id="insertManager">
        insert into manager(id,name,num,password)
            values (null,#{name},#{num},#{password})
    </insert>
    <update id="updateManager" parameterType="Manager">
        update manager
            <set>
                <if test="num!=null and num!=''">num=#{num},</if>
                <if test="password!=null and password!=''">password=#{password},</if>
            </set>
        where id=#{id}
    </update>
    <delete id="deleteManager">
        delete from manager where id=#{id}
    </delete>
    <select id="selectManager" resultType="Manager">
        select * from manager where id=#{id}
    </select>
    <select id="selectAllManager" resultType="Manager">
        select * from manager
    </select>
    <select id="selectPassword" resultType="java.lang.String">
        select password from manager where id=#{id}
    </select>
    <select id="hasManager" resultType="java.lang.Integer">
        select count(*) from manager where id=#{id}
    </select>
</mapper>